spring:
  application:
    name: aws

  datasource:
    url: jdbc:postgresql://db_aws_host:5433/db_aws
#    url: jdbc:postgresql://localhost:5433/db_aws
    username: postgres
    password: 1

    flyway:
      baselineOnMigrate: true

